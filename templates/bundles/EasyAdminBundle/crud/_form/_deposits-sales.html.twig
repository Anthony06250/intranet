{{ form_start(form) }}
    <article class="card">
        <article class="card-body">
            <section class="row">
                <div class="col-6" hidden></div>
                {{ form_row(form.user) }}
                {{ form_row(form.store) }}
                {{ form_row(form.product) }}
                {{ form_row(form.serial_number) }}
                {{ form_row(form.status) }}
                {{ form_row(form.reserved_price) }}
                {{ form_row(form.created_at) }}
            </section>
        </article>
    </article>
    <article class="card">
        <article class="card-body">
            <section class="row">
                {{ form_row(form.customer) }}
            </section>
        </article>
    </article>
    <article class="card">
        <article class="card-body">
            <section class="row">
                {{ form_row(form.comments) }}
            </section>
        </article>
    </article>
{{ form_end(form) }}
